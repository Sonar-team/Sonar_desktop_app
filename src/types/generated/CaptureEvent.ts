// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { GraphData } from "./GraphData";
import type { GraphUpdate } from "./GraphUpdate";
import type { PacketBatchPacket } from "./PacketBatchPacket";
import type { Stats } from "./Stats";

/**
 * Contrat exhaustif généré pour `src/types/generated/CaptureEvent.ts`,
 * consommé tel quel par `src/types/capture.ts`. `camelCase` uniforme (tags
 * de variante ET champs de struct, `rename_all`/`rename_all_fields`, #142) —
 * seule exception assumée : les champs qui traversent la couche paquet/flux
 * de la crate vendorée `packet_parser` (`PacketFlowContract` et ce qu'elle
 * contient) restent dans la casse qu'elle impose, cf. le commentaire de tête
 * de module.
 *
 * Pas de variante `Packet` : `CaptureEvent::Packet` n'est construite nulle
 * part (`#[allow(dead_code)]` côté Rust) — plutôt qu'un DTO jamais exercé,
 * le variant est retiré des deux côtés (voir aussi le nettoyage du store
 * frontend, #142).
 */
export type CaptureEvent = { "event": "started", "data": { sessionId: number, device: string, bufferSize: number, chanCapacity: number, timeout: number, snaplen: number, linkType: string, protocolVersion: number, } } | { "event": "stats", "data": Stats } | { "event": "channelCapacityPayload", "data": { sessionId: number, channelSize: number, currentSize: number, backpressure: boolean, } } | { "event": "packetBatch", "data": { sessionId: number, packets: Array<PacketBatchPacket>, } } | { "event": "graph", "data": { update: GraphUpdate, } } | { "event": "graphBatch", "data": { sessionId: number, updates: Array<GraphUpdate>, } } | { "event": "stopped", "data": { sessionId: number, reason: string, } } | { "event": "finished", "data": { fileName: string, packetTotalCount: number, integratedCount: number, parseErrorCount: number, matrixTotalCount: number, } } | { "event": "importProgress", "data": { fileName: string, fileIndex: number, filesTotal: number, current: number, total: number, } } | { "event": "graphSnapshot", "data": { graphData: GraphData, } };
